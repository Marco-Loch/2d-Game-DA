class Level {
  enemys;
  clouds;
  backgroundObjects;

  constructor(enemys, clouds, backgroundObjects) {
    this.enemys = enemys;
    this.clouds = clouds;
    this.backgroundObjects = backgroundObjects;
  }
}
